# Scss-Lint configuration overrides
# https://github.com/causes/scss-lint

linters:
  NameFormat:
    convention: BEM
  NestingDepth:
    max_depth: 5
  PropertySortOrder:
    order: smacss
  SelectorFormat:
    convention: hyphenated_BEM
